# starship

Cross-shell prompt. Config: `starship.toml`.

- Minimal left side: directory + prompt character only
- Everything else (git, languages, cloud) sits on the right
- catppuccin_mocha palette, vi-mode indicator shows `[N] >>>`
- `add_newline = false` keeps prompts compact

# =============================================================================
#  Tmux configuration - THE BRAILLE HACK & EDGE LOCK
# =============================================================================

# ─── Base settings ───────────────────────────────────────────────────────────
source-file ~/.tmux.reset.conf
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"

set -g prefix ^A
set -g base-index 1                     # start indexing windows at 1 instead of 0
set -g detach-on-destroy off            # don't exit from tmux when closing a session
set -g renumber-windows on              # renumber all windows when any window is closed
set -g set-clipboard on                 # use system clipboard
set -g status-position bottom           # macOS / darwin style
set -g escape-time 0                    # set the escape time to 0 milliseconds
set -g default-terminal "${TERM}"
setw -g mode-keys vi

set -g pane-active-border-style 'fg=magenta,bg=default'
set -g pane-border-style 'fg=brightblack,bg=default'

set -g @fzf-url-fzf-options '-p 60%,30% --prompt="   " --border-label=" Open URL "'
set -g @fzf-url-history-limit '2000'

# ─── Plugins ─────────────────────────────────────────────────────────────────
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux#v2.1.0'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'fcsonline/tmux-thumbs'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'wfxr/tmux-fzf-url'
set -g @plugin 'omerxx/catppuccin-tmux'
set -g @plugin 'omerxx/tmux-sessionx'
set -g @plugin 'omerxx/tmux-floax'
set -g @floax-width '80%'
set -g @floax-height '80%'
set -g @floax-border-color 'magenta'
set -g @floax-text-color 'blue'
set -g @floax-bind 'p'
set -g @floax-change-path 'true'
set -g @sessionx-bind-zo-new-window 'ctrl-y'
set -g @sessionx-auto-accept 'off'
set -g @sessionx-bind 'o'
set -g @sessionx-x-path '~/dotfiles'
set -g @sessionx-window-height '85%'
set -g @sessionx-window-width '75%'
set -g @sessionx-zoxide-mode 'on'
set -g @sessionx-custom-paths-subdirectories 'false'
set -g @sessionx-filter-current 'false'
set -g @continuum-restore 'off'
set -g @resurrect-strategy-nvim 'session'

# ─── Catppuccin Flavor ──────────────────────────────────────────────────────
set -g @catppuccin_flavor 'mocha'
set -g @catppuccin_status_background "none"

run '~/.tmux/plugins/tmux/catppuccin.tmux'
run '~/.tmux/plugins/tpm/tpm'

# ─── THE FINAL POLISH OVERRIDES ──────────────────────────────────────────────

set -g status-bg default
set -g status-style "bg=default"

# 1. Extreme Left: Session Pill (FLUSH LEFT)
set -g status-left-length 100
set -g status-left "#[fg=#{@thm_crust},bg=#{@thm_green},bold]  #[fg=#{@thm_fg},bg=#{@thm_surface_0},nobold] #S #[fg=#{@thm_surface_0},bg=default] "

# 2. Middle: Window Pills
set -g window-status-separator " "
set -g window-status-format "#[fg=#{@thm_surface_0},bg=default]#[fg=#{@thm_fg},bg=#{@thm_surface_0}] #I #[fg=#{@thm_fg},bg=#{@thm_surface_1}] #W #[fg=#{@thm_surface_1},bg=default]"
set -g window-status-current-format "#[fg=#{@thm_peach},bg=default]#[fg=#{@thm_crust},bg=#{@thm_peach},bold] #I #[fg=#{@thm_fg},bg=#{@thm_surface_0},bold] #W #[fg=#{@thm_surface_0},bg=default]"

# 3. Extreme Right: Directory Pill (STRICT FLUSH RIGHT)
# FIX: The string ends with a Braille Blank (U+2800) instead of a normal space.
# This forces the terminal to render the background block without trimming it.
set -g status-right-length 100
set -g status-right "#[fg=#{@thm_pink},bg=default]#[fg=#{@thm_crust},bg=#{@thm_pink}] #[fg=#{@thm_fg},bg=#{@thm_surface_0}] #{b:pane_current_path}⠀"

# set -g @catppuccin_meetings_text "#($HOME/.config/tmux/scripts/cal.sh)"
# set -g @catppuccin_date_time_text "%H:%M"
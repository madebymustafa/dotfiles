# television

Fuzzy finder TUI. `config.toml` is base settings; `cable/` defines the
channels you can search: files, dirs, dotfiles, git branches/logs,
zoxide jumps, env vars, downloads, brew packages and aliases.

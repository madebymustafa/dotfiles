# sesh

Smart tmux session manager, wired into `.tmux.conf` keybindings.
Config: `sesh.toml`.

- Default session starts nvim with Telescope find_files
- Previews rendered with eza
- Sessions defined for `home` and `dotfiles`, plus zoxide/dir discovery

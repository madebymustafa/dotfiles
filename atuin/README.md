# atuin

Synced, searchable shell history replacement. Config: `config.toml`.

- Compact list style, Enter accepts the highlighted command
- History records sync enabled (end-to-end encrypted by default)
